alias ls='exa'
alias ll='exa -alF'
alias la='exa -A'
alias l='exa -F'

alias TMS="date -u +\"%Y-%m-%dT%H:%M:%S.%3N\""

alias nnn='nnn -a'

if [ -n $WAYLAND_DISPLAY ]; then 
    NNN_PLUG_PREVIEW='p:preview-tui'
else 
    NNN_PLUG_PREVIEW='p:preview-tabbed'
fi

NNN_PLUG_DEFAULT="$NNN_PLUG_PREVIEW;p:preview-tui;d:diffs"
NNN_PLUG="$NNN_PLUG_DEFAULT"

export NNN_PLUG

if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

[[ -f ~/._private_profile ]] && source ~/._private_profile
